
<h1 class="Editing">Editing Book</h1>

<!--エラーメッセージ-->
<% if @book.errors.any? %>
  <div class="alert_alert-warning">
  	<h2><%= pluralize(@book.errors.count, "error") %> prohibited this product from being saved:</h2>
    <ul>
      <% @book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
<!--エラーメッセージ-->



<!-- form_for部分 -->
<%= form_for(@book) do |f| %>
  <h4>Title</h4>
  <div class="Editing">
  	<%= f.text_field :title %>
  </div>
  <h4>Body</h4>
  <div class="Editing">
  	<%= f.text_area :body %>
  </div>
  <div class="Update">
  	<%= f.submit 'Update Book' %>
  </div>

 <div class="Pass">
	<%= link_to "Show", book_path %> |
	<%= link_to "Back", books_path %>
 </div>
<% end %>